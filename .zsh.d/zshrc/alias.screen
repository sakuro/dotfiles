#!/bin/zsh

if in_screen; then
  alias irb='screen -t irb $(rbenv which irb)'
  if is_executable vim; then
    alias vi='screen vim'
  else
    alias vi='screen vi'
  fi
  function man() { screen -t "man $1" man $* }
  function slogin() { screen -t '@'$1 command slogin $* }
fi
