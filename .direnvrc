use_bundler() {
  local dir
  [[ -f Gemfile.lock ]] || return 0
  for dir in vendor/bundle/bin bin; do
    PATH_add $dir
  done
}

use_ruby() {
  local dir
  PATH_add $HOME/.rubies/ruby-$1/bin
  [[ -f Gemfile.lock ]] || return 0
  use bundler
}
