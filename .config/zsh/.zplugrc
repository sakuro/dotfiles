#!/bin/zsh

export ZPLUG_HOME=$ZDOTDIR/plugins/zplug
export ZPLUG_REPOS=$ZDOTDIR/plugins
export ZPLUG_LOADFILE=$ZDOTDIR/.zplug-packages

source $ZPLUG_HOME/init.zsh

if ! zplug check --verbose; then
  printf 'Install? [y/N]: '
  if read -q; then
    echo; zplug install
  fi
fi

zplug load
